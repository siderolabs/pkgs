name: libtasn1
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - image: "{{ .TOOLS_PREFIX }}tools-libtasn1:{{ .TOOLS_REV }}"
    to: /rootfs
steps:
  - install:
      - |
        rm -rf /rootfs/usr/share/info /rootfs/usr/share/man
  - test:
      - |
        fhs-validator /rootfs
finalize:
  - from: /rootfs
    to: /
