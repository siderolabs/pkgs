name: kernel-prepare
variant: scratch
shell: /toolchain/bin/bash
dependencies:
  - image: '{{ .TOOLS_IMAGE }}'
steps:
  - sources:
      - url: https://cdn.kernel.org/pub/linux/kernel/v5.x/linux-5.8.10.tar.xz
        destination: linux.tar.xz
        sha256: 9d6e126a7fad618a477540e242f148f7d61e0116b500312f788ac63e126b5af2
        sha512: cff8cbbf30b8a8b3e151573d36dd6a36e3ddf849f1866f02c811c30a95945d76752051aff6c5b680d19d72dc42a6238ba438f3e00755c1eb64b5ba0276fbad0f
    env:
      ARCH: {{ if eq .ARCH "aarch64"}}arm64{{ else if eq .ARCH "x86_64" }}x86_64{{ else }}unsupported{{ end }}
    prepare:
      - |
        tar -xJf linux.tar.xz --strip-components=1
        rm linux.tar.xz

        mkdir /bin
        ln -sv /toolchain/bin/bash /bin/bash
        ln -sv /toolchain/bin/bash /bin/sh
        ln -sv /toolchain/lib /lib
        mkdir -p /usr/bin \
            && ln -sf /toolchain/bin/env /usr/bin/env \
            && ln -sf /toolchain/bin/true /bin/true \
            && ln -sf /toolchain/bin/false /bin/false \
            && ln -sf /toolchain/bin/pwd /bin/pwd

        # Ensure that `make menuconfig` works.

        ln -s /toolchain/bin/awk /usr/bin/awk

        mkdir -p /usr/lib/pkgconfig
        ln -s /toolchain/include /usr/include

        for lib in ncurses form panel menu ; do
            rm -vf                    /lib/lib${lib}.so
            echo "INPUT(-l${lib}w)" > /lib/lib${lib}.so
            ln -sfv ${lib}w.pc        /lib/pkgconfig/${lib}.pc
        done

        make mrproper
    install:
      - |
        mkdir -p /src
        cp -a . /src/
finalize:
  - from: /src
    to: /src
  - from: /toolchain
    to: /toolchain
  - from: /usr
    to: /usr
  - from: /bin
    to: /bin
  - from: /lib
    to: /lib
