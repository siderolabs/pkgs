name: gnutls
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - image: "{{ .TOOLS_PREFIX }}tools-gnutls:{{ .TOOLS_REV }}"
    to: /rootfs
steps:
  - install:
      - |
        rm -rf /rootfs/usr/share/locale
  - test:
      - |
        fhs-validator /rootfs
finalize:
  - from: /rootfs
    to: /
