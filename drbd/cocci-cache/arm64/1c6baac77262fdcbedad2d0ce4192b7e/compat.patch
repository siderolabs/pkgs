--- drbd_transport_tcp.c
+++ /tmp/cocci-output-1424-a4b97f-drbd_transport_tcp.c
@@ -511,15 +511,15 @@ static int dtt_try_connect(struct dtt_pa
 	*  a free one dynamically.
 	*/
 	what = "bind before connect";
-	err = socket->ops->bind(socket, (struct sockaddr_unsized *) &my_addr,
-			path->path.my_addr_len);
+	err = socket->ops->bind(socket, (struct sockaddr *) &my_addr,
+				path->path.my_addr_len);
 	if (err < 0)
 		goto out;
 
 	/* connect may fail, peer not yet available.
 	 * stay C_CONNECTING, don't go Disconnecting! */
 	what = "connect";
-	err = socket->ops->connect(socket, (struct sockaddr_unsized *) &peer_addr,
+	err = socket->ops->connect(socket, (struct sockaddr *) &peer_addr,
 				   path->path.peer_addr_len, 0);
 	if (err < 0) {
 		err = dtt_socket_err_to_eagain(err);
@@ -1065,7 +1065,8 @@ static int dtt_init_listener(struct drbd
 	addr_len = addr->sa_family == AF_INET6 ? sizeof(struct sockaddr_in6)
 		: sizeof(struct sockaddr_in);
 
-	err = s_listen->ops->bind(s_listen, (struct sockaddr_unsized *)&my_addr, addr_len);
+	err = s_listen->ops->bind(s_listen, (struct sockaddr *)&my_addr,
+				  addr_len);
 	if (err < 0) {
 		what = "bind before listen";
 		goto out;
@@ -1560,12 +1561,12 @@ static int dtt_send_page(struct drbd_tra
 			struct page *p = page + (offset >> PAGE_SHIFT);
 			unsigned int poff = offset & ~PAGE_MASK;
 			unsigned int chunk = min_t(int, len, PAGE_SIZE - poff);
-			void *addr = kmap_local_page(p);
+			void *addr = kmap(p);
 			struct kvec iov = { .iov_base = addr + poff, .iov_len = chunk };
 
 			iov_iter_kvec(&msg.msg_iter, ITER_SOURCE, &iov, 1, chunk);
 			sent = sock_sendmsg(socket, &msg);
-			kunmap_local(addr);
+			kunmap(p);
 		} else {
 			bvec_set_page(&bvec, page, len, offset);
 			iov_iter_bvec(&msg.msg_iter, ITER_SOURCE, &bvec, 1, len);
--- drbd_transport_rdma.c
+++ /tmp/cocci-output-1424-61a28d-drbd_transport_rdma.c
@@ -3185,9 +3185,9 @@ static int dtr_send_page(struct drbd_tra
 			kfree(tx_desc);
 			return -ENOMEM;
 		}
-		from = kmap_local_page(caller_page);
+		from = kmap(caller_page);
 		memcpy(page_address(page), from + offset, size);
-		kunmap_local(from);
+		kunmap(caller_page);
 		offset = 0;
 	}
 
--- drbd_transport_lb-tcp.c
+++ /tmp/cocci-output-1424-dd6ce6-drbd_transport_lb-tcp.c
@@ -530,7 +530,7 @@ dtl_recv(struct drbd_transport *transpor
 static int
 _dtl_recv_page(struct dtl_transport *dtl_transport, struct page *page, int size)
 {
-	void *data = kmap_local_page(page);
+	void *data = kmap(page);
 	void *pos = data;
 	struct dtl_flow *flow;
 	int err;
@@ -549,7 +549,7 @@ _dtl_recv_page(struct dtl_transport *dtl
 	}
 	err = pos - data;
 out:
-	kunmap_local(data);
+	kunmap(page);
 	return err;
 }
 
@@ -694,7 +694,8 @@ dtl_try_connect(struct drbd_transport *t
 	 * a free one dynamically.
 	 */
 	what = "bind before connect";
-	err = sock->ops->bind(sock, (struct sockaddr_unsized *) &my_addr, path->path.my_addr_len);
+	err = sock->ops->bind(sock, (struct sockaddr *) &my_addr,
+			      path->path.my_addr_len);
 	if (err < 0)
 		goto out;
 
@@ -703,7 +704,7 @@ dtl_try_connect(struct drbd_transport *t
 	mutex_lock(&dtl_transport->connecting_socket_mutex);
 	dtl_transport->connecting_socket = sock;
 	mutex_unlock(&dtl_transport->connecting_socket_mutex);
-	err = sock->ops->connect(sock, (struct sockaddr_unsized *) &peer_addr,
+	err = sock->ops->connect(sock, (struct sockaddr *) &peer_addr,
 				   path->path.peer_addr_len, 0);
 	mutex_lock(&dtl_transport->connecting_socket_mutex);
 	dtl_transport->connecting_socket = NULL;
@@ -1171,7 +1172,8 @@ static int dtl_init_listener(struct drbd
 	addr_len = addr->sa_family == AF_INET6 ? sizeof(struct sockaddr_in6)
 		: sizeof(struct sockaddr_in);
 
-	err = s_listen->ops->bind(s_listen, (struct sockaddr_unsized *)&my_addr, addr_len);
+	err = s_listen->ops->bind(s_listen, (struct sockaddr *)&my_addr,
+				  addr_len);
 	if (err < 0) {
 		what = "bind before listen";
 		goto out;
--- drbd_sender.c
+++ /tmp/cocci-output-1424-8b801e-drbd_sender.c
@@ -835,7 +835,7 @@ struct fifo_buffer *fifo_alloc(unsigned
 {
 	struct fifo_buffer *fb;
 
-	fb = kzalloc_flex(*fb, values, fifo_size, GFP_NOIO);
+	fb = kzalloc(struct_size(fb, values, fifo_size), GFP_NOIO);
 	if (!fb)
 		return NULL;
 
--- drbd_req.c
+++ /tmp/cocci-output-1424-c7c8cb-drbd_req.c
@@ -143,7 +143,7 @@ int w_queue_peer_ack(struct drbd_work *w
 
 	list_for_each_entry_safe(req, tmp, &work_list, list) {
 		struct drbd_peer_ack *peer_ack =
-			kzalloc_obj(struct drbd_peer_ack);
+			kzalloc_obj(struct drbd_peer_ack, GFP_KERNEL);
 
 		peer_ack->resource = resource;
 		INIT_LIST_HEAD(&peer_ack->list);
--- drbd_receiver.c
+++ /tmp/cocci-output-1424-bf9bf4-drbd_receiver.c
@@ -5888,7 +5888,7 @@ static int receive_protocol(struct drbd_
 		}
 	}
 
-	new_net_conf = kmalloc_obj(struct net_conf);
+	new_net_conf = kmalloc_obj(struct net_conf, GFP_KERNEL);
 	if (!new_net_conf)
 		goto disconnect;
 
@@ -6067,7 +6067,8 @@ static int receive_SyncParam(struct drbd
 		}
 
 		if (verify_tfm || csums_tfm) {
-			new_net_conf = kzalloc_obj(struct net_conf);
+			new_net_conf = kzalloc_obj(struct net_conf,
+						   GFP_KERNEL);
 			if (!new_net_conf)
 				goto disconnect;
 
@@ -6350,7 +6351,8 @@ static int receive_sizes(struct drbd_con
 		if (my_usize != p_usize) {
 			struct disk_conf *old_disk_conf, *new_disk_conf;
 
-			new_disk_conf = kzalloc_obj(struct disk_conf);
+			new_disk_conf = kzalloc_obj(struct disk_conf,
+						    GFP_KERNEL);
 			if (!new_disk_conf) {
 				err = -ENOMEM;
 				goto out;
@@ -7698,7 +7700,7 @@ drbd_commit_size_change(struct drbd_devi
 		drbd_info(device, "New u_size %llu sectors\n",
 			  (unsigned long long)tr->user_size);
 
-		new_disk_conf = kzalloc_obj(struct disk_conf);
+		new_disk_conf = kzalloc_obj(struct disk_conf, GFP_KERNEL);
 		if (!new_disk_conf) {
 			device->ldev->disk_conf->disk_size = tr->user_size;
 			goto cont;
--- drbd_nl.c
+++ /tmp/cocci-output-1424-35d035-drbd_nl.c
@@ -433,7 +433,7 @@ static int drbd_pre_doit(const struct ge
 	unsigned int flags;
 	int err;
 
-	adm_ctx = kzalloc_obj(struct drbd_config_context);
+	adm_ctx = kzalloc_obj(struct drbd_config_context, GFP_KERNEL);
 	if (!adm_ctx)
 		return -ENOMEM;
 
@@ -2534,7 +2534,7 @@ static int drbd_adm_disk_opts(struct sk_
 		goto out;
 	}
 
-	new_disk_conf = kmalloc_obj(struct disk_conf);
+	new_disk_conf = kmalloc_obj(struct disk_conf, GFP_KERNEL);
 	if (!new_disk_conf) {
 		retcode = ERR_NOMEM;
 		goto fail;
@@ -3348,14 +3348,14 @@ static int drbd_adm_attach(struct sk_buf
 	}
 
 	/* allocation not in the IO path, drbdsetup context */
-	nbc = kzalloc_obj(struct drbd_backing_dev);
+	nbc = kzalloc_obj(struct drbd_backing_dev, GFP_KERNEL);
 	if (!nbc) {
 		retcode = ERR_NOMEM;
 		goto fail;
 	}
 	spin_lock_init(&nbc->md.uuid_lock);
 
-	new_disk_conf = kzalloc_obj(struct disk_conf);
+	new_disk_conf = kzalloc_obj(struct disk_conf, GFP_KERNEL);
 	if (!new_disk_conf) {
 		retcode = ERR_NOMEM;
 		goto fail;
@@ -4238,7 +4238,7 @@ static int drbd_adm_net_opts(struct sk_b
 		goto out_no_adm_mutex;
 	}
 
-	new_net_conf = kzalloc_obj(struct net_conf);
+	new_net_conf = kzalloc_obj(struct net_conf, GFP_KERNEL);
 	if (!new_net_conf) {
 		retcode = ERR_NOMEM;
 		goto out;
@@ -4403,7 +4403,8 @@ static int drbd_adm_peer_device_opts(str
 	}
 	mutex_lock(&adm_ctx->resource->conf_update);
 
-	new_peer_device_conf = kzalloc_obj(struct peer_device_conf);
+	new_peer_device_conf = kzalloc_obj(struct peer_device_conf,
+					   GFP_KERNEL);
 	if (!new_peer_device_conf)
 		goto fail;
 
@@ -4494,7 +4495,7 @@ int drbd_create_peer_device_default_conf
 	struct peer_device_conf *conf;
 	int err;
 
-	conf = kzalloc_obj(*conf);
+	conf = kzalloc_obj(*conf, GFP_KERNEL);
 	if (!conf)
 		return -ENOMEM;
 
@@ -4649,7 +4650,7 @@ static int adm_new_connection(struct drb
 	struct drbd_transport *transport;
 
 	/* allocation not in the IO path, drbdsetup / netlink process context */
-	new_net_conf = kzalloc_obj(*new_net_conf);
+	new_net_conf = kzalloc_obj(*new_net_conf, GFP_KERNEL);
 	if (!new_net_conf)
 		return ERR_NOMEM;
 
@@ -5597,7 +5598,7 @@ static int drbd_adm_resize(struct sk_buf
 	u_size = rcu_dereference(device->ldev->disk_conf)->disk_size;
 	rcu_read_unlock();
 	if (u_size != (sector_t)rs.resize_size) {
-		new_disk_conf = kmalloc_obj(struct disk_conf);
+		new_disk_conf = kmalloc_obj(struct disk_conf, GFP_KERNEL);
 		if (!new_disk_conf) {
 			retcode = ERR_NOMEM;
 			goto fail_ldev;
--- drbd_main.c
+++ /tmp/cocci-output-1424-0acd30-drbd_main.c
@@ -3787,7 +3787,7 @@ struct drbd_resource *drbd_create_resour
 {
 	struct drbd_resource *resource;
 
-	resource = kzalloc_obj(struct drbd_resource);
+	resource = kzalloc_obj(struct drbd_resource, GFP_KERNEL);
 	if (!resource)
 		goto fail;
 	resource->name = kstrdup(name, GFP_KERNEL);
@@ -3872,7 +3872,7 @@ struct drbd_connection *drbd_create_conn
 	if (drbd_alloc_send_buffers(connection))
 		goto fail;
 
-	connection->current_epoch = kzalloc_obj(struct drbd_epoch);
+	connection->current_epoch = kzalloc_obj(struct drbd_epoch, GFP_KERNEL);
 	if (!connection->current_epoch)
 		goto fail;
 
@@ -4036,7 +4036,7 @@ struct drbd_peer_device *create_peer_dev
 	struct drbd_peer_device *peer_device;
 	int err;
 
-	peer_device = kzalloc_obj(struct drbd_peer_device);
+	peer_device = kzalloc_obj(struct drbd_peer_device, GFP_KERNEL);
 	if (!peer_device)
 		return NULL;
 
@@ -4155,7 +4155,7 @@ enum drbd_ret_code drbd_create_device(st
 		return ERR_MINOR_OR_VOLUME_EXISTS;
 
 	/* GFP_KERNEL, we are outside of all write-out paths */
-	device = kzalloc_obj(struct drbd_device);
+	device = kzalloc_obj(struct drbd_device, GFP_KERNEL);
 	if (!device)
 		return ERR_NOMEM;
 	kref_init(&device->kref);
--- drbd_debugfs.c
+++ /tmp/cocci-output-1424-3cf630-drbd_debugfs.c
@@ -1984,6 +1984,10 @@ static const struct file_operations drbd
 
 static int drbd_compat_show(struct seq_file *m, void *ignored)
 {
+	seq_puts(m, "alloc_obj_default_gfp__no_present\n");
+	seq_puts(m, "kzalloc_flex__no_present\n");
+	seq_puts(m, "kmap_local_page__no_present\n");
+	seq_puts(m, "struct_sockaddr_unsized__no_present\n");
 	return 0;
 }
 
--- drbd_bitmap.c
+++ /tmp/cocci-output-1424-549f47-drbd_bitmap.c
@@ -415,7 +415,7 @@ struct drbd_bitmap *drbd_bm_alloc(unsign
 	if (max_peers < 1 || max_peers > DRBD_PEERS_MAX)
 		return NULL;
 
-	b = kzalloc_obj(struct drbd_bitmap);
+	b = kzalloc_obj(struct drbd_bitmap, GFP_KERNEL);
 	if (!b)
 		return NULL;
 
