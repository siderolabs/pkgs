name: elfutils
variant: scratch
shell: /bin/bash
dependencies:
  - stage: base
  - image: "{{ .TOOLS_PREFIX }}tools-elfutils:{{ .TOOLS_REV }}"
    to: /rootfs
steps:
  - install:
      - |
        rm -rf /rootfs/usr/share/man /rootfs/usr/share/locale
  - test:
      - |
        fhs-validator /rootfs
finalize:
  - from: /rootfs
    to: /
